{
  "//": "cuberhaus reproducible dev environment (c++ core + rust backend, e.g. pracpro2). Managed source: cuberhaus-workspace/assets/devcontainers/cpp-rust. Applied per repo by scripts/apply-devcontainers.ps1.",
  "name": "cuberhaus cpp + rust",
  "image": "mcr.microsoft.com/devcontainers/cpp:1-debian-12",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {},
    "ghcr.io/devcontainers/features/github-cli:1": {},
    "ghcr.io/devcontainers/features/rust:1": {}
  },
  "remoteUser": "vscode",
  "postCreateCommand": "bash -lc 'if [ -f skills-lock.json ]; then make skills-restore || true; fi; if [ -f Cargo.toml ]; then cargo fetch || true; fi'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools",
        "rust-lang.rust-analyzer"
      ]
    }
  }
}
