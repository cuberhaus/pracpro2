

        {
            for
            for() {
                distmap(it).(it)
                cluster e
                BinTree tre(e)
                clustmap.insert(id,distmap(it)
            }
        }
